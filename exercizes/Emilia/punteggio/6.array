extern echo(double);

fun man(input)
    with array arrof 10, v=1 do
        for i=0, i<10, 1 do
            v = v*input:
            array[i] = v
        end:
        for i=0, i<10, 1 do
            echo(array[i])
        end
    end
;